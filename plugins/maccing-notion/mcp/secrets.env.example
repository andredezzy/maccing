# Copy to `secrets.env` (gitignored) and fill in your token, OR `export NOTION_TOKEN=...`
# in your shell — .mcp.json forwards it. The token is a Notion internal-integration
# Personal Access Token: notion.so/profile/integrations -> Personal access tokens.
export NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
