{
  "name": "maccing",
  "private": true,
  "type": "module",
  "description": "Maccing plugin marketplace — repo-level tooling (Biome lint/format).",
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16"
  }
}
