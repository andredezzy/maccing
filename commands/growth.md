---
description: Growth orchestrator — paid acquisition and owned-messaging strategy and execution
---

Use the `growth` skill for this task: $ARGUMENTS

If you are in a project repo, first read its growth state under `.maccing/growth/` (see the skill's Iron Laws), then route to the relevant platform skill: `growth-google-ads`, `growth-meta-ads`, `growth-tiktok-ads`, or `growth-meta-whatsapp` (the latter two load the shared `growth-meta` skill first; YCloud BSP ops live in `growth-ycloud` / `growth-ycloud-api`).
